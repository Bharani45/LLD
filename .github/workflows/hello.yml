name: First workflow
on:
  push:
    branches:
      - main
jobs:
  hello-job:
    runs-on: ubuntu-latest
    steps:
      - name: print hello
        run: echo "hello from Github actions"

      - name: Show system info
        run: | 
            whoami
            pwd
            uname -a